# Copy to `.env` (done automatically by `make up`). Development credentials only:
# the stack listens on 127.0.0.1 and never leaves the machine.
MINIO_ROOT_USER=medallion
MINIO_ROOT_PASSWORD=medallion-local-dev
